# CellSim
Simple Python cell simulator using pymunk and pyglet.

# Usage
To use CellSim, run the following command:
~~~
    python cellsim.py <config_file>
~~~

Where the config file must look like the following:
